<div class="grid_2">
  <h3>Kategorie</h3>
</div>
<div class="grid_10">
  <% for work in @works do %>
    <div>
      <h3><%= work.name %></h3>
      <%= image_tag work.image.url(:medium) if work.image %>
      <%= work.description %>
    </div>
    <hr />
   <% end %>
</div>
